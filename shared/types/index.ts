export type Trip = {
  id: string;
  title: string;
  destination: string;
};

export type User = {
  id: string;
  email: string;
};

export type Expense = {
  id: string;
  amount: number;
  category: string;
};
